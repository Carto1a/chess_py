/* #include "window.h" */
/* #include <SDL2/SDL.h> */

#include <stdio.h>
int mains() {
  /* Window *window = Window_Construct(); */
  /**/
  /* while (window->Is_Running(window)) { */
  /*   window->Process_Events(window); */
  /*   window->Draw(window); */
  /* } */
  /**/
  /* Window_Desconstruct(window); */
  int i = 0;
  if (i) {
    printf("teste debug");
  } else {
    printf("teste debug 2");
  }

  return 1;
}
